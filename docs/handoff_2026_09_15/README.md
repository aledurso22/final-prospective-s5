# Cluster update packet

Read DERIVATION_CONTRACT.md first, then CLUSTER_CODING_BRIEF.md. The user's latest instruction is fixed added physical coefficients, faithful equation-to-code mapping, ordinary S5 learning, and training on the cluster only. A favorable score does not justify changing the derivation.

The main physical candidate retains dendritic mass. M=0 is an explicitly labeled reduced-model ablation. Reproduce Rawat's alpha-P-S5 as a distinct baseline, including its gain/clipping and model/data/training recipe. Prepare and execute the bounded cluster protocol; do not run new local training.

The current source checkout was inspected clean at a4c5b12599c247923ee0425c25a09d92e8556ce3. Two runner gaps were directly observed: absent checkpoint writes and incorrect state-coordinate count. Numerical tests were not rerun for this packet. No cluster job was submitted, no message was sent into Claude Code, and no production source was edited by the coordinator.

References are verbatim copies with original source paths and hashes in inspection_record.json. They preserve historical results and assumptions; this new contract governs the current plan. Some older reports link to figures/scripts outside the copied packet. The original paths remain authoritative for those artifacts.

The coding agent should copy the governing brief/contract and necessary source attribution into its implementation branch before committing the new protocol. Push code and reports to the existing public repository; retain datasets and large checkpoints in cluster artifact storage. Return the actual new commit, run identity and evidence.
